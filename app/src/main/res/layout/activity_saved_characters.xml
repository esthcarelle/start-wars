<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/stars"
    tools:context=".SavedCharacters">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/charact"
        android:text="AWESOME SAVED CHARACTERS"
        android:fontFamily="@font/gentium_book_basic"
        android:textColor="@color/white"
        android:textSize="30dp"
        android:textAlignment="center"
        android:layout_margin="10dp"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Click on one item if you want to delete it"
        android:textAlignment="center"
        android:id="@+id/del"
        android:textColor="@color/white"
        android:textSize="15dp"
        android:layout_below="@id/charact"
        android:layout_marginBottom="9dp"/>
    <ListView
        android:layout_below="@id/del"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/listview"/>
</RelativeLayout>